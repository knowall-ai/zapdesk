# Azure DevOps Configuration for Backlog Creator
# Copy this to .env.local in the project root OR export directly in your shell

# Personal Access Token with Work Items (Read, Write) permission
# Get one from: https://dev.azure.com/{your-org}/_usersSettings/tokens
AZURE_DEVOPS_PAT=

# Organization and project are passed as command-line arguments:
#   node scripts/create-backlog.mjs -o <org> -p <project> -t <template>
#
# Example:
#   export AZURE_DEVOPS_PAT="your_token_here"
#   node scripts/create-backlog.mjs -o KnowAllTest -p "ABC Inc (Agile)" -t agile
